#ifndef CAR_WORKSHOP_WORKER_SCHEDULER_TERMINAL_COLORS_H
#define CAR_WORKSHOP_WORKER_SCHEDULER_TERMINAL_COLORS_H

/**
 * Stores the ANSI code for the default color
 */
extern const char *TERMINAL_COLOR_DEFAULT;

/**
 * Stores the ANSI code for the color red
 */
extern const char *TERMINAL_COLOR_RED;

/**
 * Stores the ANSI code for the color green .
 */
extern const char *TERMINAL_COLOR_GREEN;

/**
 * Stores the ANSI code for the color yellow
 */
extern const char *TERMINAL_COLOR_YELLOW;

/**
 * Stores the ANSI code for the color blue
 */
extern const char *TERMINAL_COLOR_BLUE;

/**
 * Stores the ANSI code for the color magenta
 */
extern const char *TERMINAL_COLOR_MAGENTA;

/**
 * Stores the ANSI code for the color cyan
 */
extern const char *TERMINAL_COLOR_CYAN;

/**
 * Stores the ANSI code for the color white
 */
extern const char *TERMINAL_COLOR_WHITE;

#endif //CAR_WORKSHOP_WORKER_SCHEDULER_TERMINAL_COLORS_H
